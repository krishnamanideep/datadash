rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all collections for initial setup
    match /{document=**} {
      allow read, write: if true;
    }
  }
}